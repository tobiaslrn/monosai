/** Build-time constants supplied through the Angular builder `define` option. */
declare const MONOSAI_BUILD_COMMIT: string;

/** Whether this build ships a service worker; the e2e build does not. */
declare const MONOSAI_SERVICE_WORKER: boolean;
